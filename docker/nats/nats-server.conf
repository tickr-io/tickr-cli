# Client and monitoring listeners
port: 4222
http_port: 8222

# Development logging
debug: false
trace: false
logtime: true

# Persist streams used by the data plane.
jetstream {
    store_dir: "/data/jetstream"
    max_memory_store: 1G
    max_file_store: 10G
}
